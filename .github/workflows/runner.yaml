name: Deployment Runner

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: deploy frontend
        run: |
          cd /school-lms/frontend
          git pull origin main
          cd ..
          docker compose up -d --build
